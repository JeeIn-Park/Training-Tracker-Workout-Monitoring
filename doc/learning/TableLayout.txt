        <TableLayout
            android:id="@+id/todaySetTable"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

here, I want to show a table.
each time log button is clicked, it will add a set to the list.
with (set count) / ()kg * (), add a row whenever the button is clicked.
the view needs to be updated each time the log button is clicked

logButton.setOnClickListener{
            val dateTime = LocalDateTime.now()
            val massString = kgEditText.text.toString()
            val mass = massString.toFloatOrNull()
            val repString = repEditText.text.toString()
            val rep = repString.toIntOrNull()
            if (warmUpCheckBox.isChecked) {
                setNum = null
            } else {
                currentSetCount += 1
                setNum = currentSetCount
            }

            val set = ExerciseSet(
                dateTime = dateTime,
                exerciseCard = cardItem,
                mass = mass,
                set = setNum,
                rep = rep)
            exerciseSetList.add(set)
        }
    }
CX 